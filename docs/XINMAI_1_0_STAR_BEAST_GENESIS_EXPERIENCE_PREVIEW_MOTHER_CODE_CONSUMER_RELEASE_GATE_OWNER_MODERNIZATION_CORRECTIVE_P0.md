# XINMAI 1.0 — Star Beast Genesis Experience Preview / Mother Code Consumer Release Gate Owner Modernization Corrective P0

## Exit

**A. CORRECTIVE COMPLETE — PREVIEW GATE PASSES; RELEASE ADVANCED TO NEXT INDEPENDENT OWNER FAILURE**

This corrective changes Gate evidence only. Preview UI, CSS, routes, protocol, Mother Code source, Runtime, Authority, DB, Store, schema, Catalog, and product behavior are unchanged.

## Authorized change

Source audit:

- `docs/XINMAI_1_0_STAR_BEAST_GENESIS_EXPERIENCE_PREVIEW_MOTHER_CODE_CONSUMER_RELEASE_GATE_OWNERSHIP_RE_AUDIT_P0.md`
- `docs/release-gates/STAR_BEAST_GENESIS_EXPERIENCE_PREVIEW_ASSERTION_CLASSIFICATION_REGISTER_P0.md`

The Preview Gate now:

1. retains all 15 A-class Canonical Facts;
2. retains all 42 B-class Consumer Contracts;
3. modernizes the two stale B-class ownership targets from `scene model fixtures` to `real life visual adapter`;
4. retires all 18 C-class exact copy, stage-label, CSS-selector/media-query, and root-element implementation assertions;
5. contains no D-class assertions.

The active Gate therefore contains 57 Release-owned assertions: A 15 + B 42.

## Dedicated verification

`npm run check:star-beast-genesis-experience-preview`

Result: **PASS**

The Gate proves:

- isolated Preview scope;
- current Mother Code Consumer ownership;
- no Storage, network, navigation, Canvas, Three.js, Launch, Gravity, or product-route coupling;
- P86/P87 and asset-architecture boundaries;
- manual-acceptance and Release registration boundaries.

## Release lifecycle result

`npm run check:release` advanced through the corrected Preview Gate and stopped at:

`check-isolated-webgl-renderer-prototype-slice`

The next failure is independently reproducible. Its Gate provides a minimal Canvas double containing only `getContext`, while the current shared Renderer calls:

`input.canvas?.hasAttribute("data-reality-life-universe-renderer")`

The result is:

`TypeError: input.canvas?.hasAttribute is not a function`

This failure is outside the authorized Preview ownership corrective. No workaround or adjacent Gate edit is included.

## Scope proof

Modified implementation file:

- `scripts/check-star-beast-genesis-experience-preview.mjs`

Added evidence file:

- this document

Product source, Preview implementation, CSS, route, protocol, Runtime, Authority, DB, Store, schema, and Catalog diff: **0**

Push: **HOLD**

## Next exact audit

`XINMAI-1.0-ISOLATED-WEBGL-RENDERER-PROTOTYPE-SLICE-CANVAS-CAPABILITY-RELEASE-GATE-OWNERSHIP-RE-AUDIT-P0`

Traffic: **RED / AUDIT FIRST**

The next audit must classify the failing Canvas capability as Canonical Renderer contract, Consumer test-double contract, legacy prototype detail, or unknown. It must not begin by weakening the Renderer capability check.
