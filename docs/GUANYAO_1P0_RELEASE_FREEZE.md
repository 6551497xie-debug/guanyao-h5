# Guanyao 1.0 Release Freeze

## Freeze Commit

- Latest product commit: `8fddf1b feat(dynamics): add two stage crystal end page`
- Freeze status: `GUANYAO 1.0 MAIN CHAIN FROZEN`

## Frozen Production Main Chain

### ENTRY / Starbeast Entry

Production entry URLs:

- `/launch-lab?entryUser=new`
- `/launch-lab?entryUser=old`

### New User Path

```text
ENTRY
-> one primary starbeast collapse
-> origin coordinate axis
-> year / month / day / hour / province
-> mother code generation
-> mother code card front / back
-> reality pressure entry
-> pressure seed axis
-> horizontal 01 / 02 / 03 cursor locks Zhongzhou
-> /dynamics
-> six-dimension transmission
-> current crystal card
-> save to Personality Ring Lite
```

### Old User Path

```text
ENTRY
-> one primary starbeast collapse
-> pressure seed axis
-> Zhongzhou lock
-> /dynamics
-> current crystal card when mother context exists
-> save to Personality Ring Lite
```

### Direct Dynamics Safety

```text
/dynamics
-> safe fallback when context is missing
-> no fake crystal
-> no fake Personality Ring Lite state
```

## Frozen Capabilities

- Visual Timeline Engine
- `debugTimeline` internal node debug mode
- origin coordinate five-step tuning
- mother code generation: geo + chrono + starbeast
- four beast x trigram visual grammar
- mother code card front / back
- pressure seed axis
- mother context bridge
- pressure seed context bridge
- `currentHexagramProfile` bridge
- `currentCrystalEndState`
- two-stage crystal end state: `MOLD` / `CARD`
- Personality Ring Lite

## Isolated Legacy Chains

The following are not part of the active 1.0 production main chain and must remain isolated:

- archive
- yao-device
- repair method
- migration
- choice
- hexagram-stamp
- old R8 page flow
- collectible asset system
- commercial physical-card buttons

## Final Validation Commands

Required release-freeze checks:

```text
npm run build
npm run check:release
git diff --check
git status --short
```

Expected result:

- build passes
- release check passes
- diff check passes
- working tree is clean except this release-freeze document before commit

## Release Statement

Guanyao 1.0 is frozen as:

```text
LaunchLab
-> origin coordinate / mother code
-> pressure seed
-> /dynamics six-dimension transmission
-> current crystal end state
-> Personality Ring Lite
```

No old chain is reconnected by this release freeze.
