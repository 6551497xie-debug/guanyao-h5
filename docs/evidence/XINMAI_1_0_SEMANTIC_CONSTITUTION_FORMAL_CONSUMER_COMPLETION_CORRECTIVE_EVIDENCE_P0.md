# XINMAI 1.0 Semantic Constitution Formal Consumer Completion Corrective — Evidence P0

## Delivery identity

- Blade: `XINMAI-1.0-SEMANTIC-CONSTITUTION-FORMAL-CONSUMER-COMPLETION-CAUSAL-SENTENCE-AND-POST-CLOSURE-NAMING-CORRECTIVE-P0`
- Exact parent: `e353cca7a89d061ee160468a0b09ca3947a844e8`
- Push: `HOLD`
- Runtime scope: Presentation consumers, direct CSS and direct gates only.
- Authority/DB/Store/Schema/Writer changes: `0`.

## Eight blocking findings

1. **Birth** — public explanation now says the system saves the experience's unique time-coordinate record after verification; it does not claim a unique life origin or fate.
2. **Reality** — selected-scene status is direct modern language and the visible/accessibility CTA is `开始六维观察`.
3. **Six dimensions** — interactive buttons, labels, live regions and instructions use modern observation language. The six final actions are exactly:
   - `保存这次身体观察`
   - `保存这次情绪观察`
   - `保存这次想法观察`
   - `保存这次行动冲动观察`
   - `保存这次记忆联想观察`
   - `保存这次需要与方向观察`
4. **Response Map** — ACTION and GOAL use bounded-ID phrase maps. All `3 × 3 = 9` valid combinations are deterministic; `UNCERTAIN` states uncertainty and never invents a protected need. The persisted six-item mirrors remain unchanged.
5. **Departure** — the screen states that the step is saved, the real action has not happened, and the user should return after acting. A normal click/keyboard button is the route action.
6. **Ownership** — public copy says a real-world result is stored as a reviewable trace. Public status and accessibility copy do not expose internal English protocol terms.
7. **Post-closure Naming** — after a canonical body imprint exists, `可选：设置同行称呼` opens the existing Naming presentation and writer. Saving `微光` produced `可选：修改同行称呼`; `以后再说` closed the editor without blocking Fresh Reality. First Reality remains naming-free.
8. **Formal consumer sweep** — the direct gate checks production consumers/resolvers, accessible labels, six labels/actions, nine causal combinations, pre/post Naming boundaries and frozen Authority digests. Coverage: `UNKNOWN=0`.

## Clean hashed Production E2E

- Origin used: `http://127.0.0.1:5517`
- Executable assets observed:
  - `/assets/index-B8NT33_f.js`
  - `/assets/index-D-UrZTEm.css`
  - `/assets/xinmai-same-life-surface-CgiZitck.css`
  - `/assets/RealityLifeUniverseCanvas-G1oFoqL-.css`
- `/@vite/client`: `0`
- `/src/main.tsx`: `0`
- Runtime console errors/warnings: `0`

The visible product path completed:

`Birth 1990-01-15 10:00 → Genesis → Whisper skip → Reality → BODY/EMOTION/THOUGHT/ACTION/MEMORY/GOAL → exact Response Map → Choice → Departure → ATTEMPTED Return → one trace → Ownership → Naming save/skip → Fresh Reality`.

Evidence observed:

- Birth public copy is non-deterministic and the confirmation became enabled from native date/time values.
- Reality exposed a visible `开始六维观察` button.
- Six final actions were visible and the canonical count advanced `0 → 6` only after final acknowledgements.
- The Response Map recovered all six saved choices.
- The causal sentence was: `你最先想推进，同时有一个重要需要不想被忽略。下一次可以先尝试【下一次当前工作或权责场景出现时，先停一口呼吸，再决定是否沿用原来的反应。】。`
- One attempted real response produced one reviewable trace.
- Optional Naming saved and recovered the name without changing lifecycle readiness.
- Fresh Reality foregrounded a different eligible scene and marked the completed scene `刚刚完成过的一幕`.
- Existing direct no-fact gates prove `NOT_ATTEMPTED` and `USER_REJECTED_RECORD` remain zero Fact/Crystal and retain both truthful exits.

## Engineering evidence

- TypeScript: `PASS`
- Production Build: `PASS` (`420 modules`)
- Full registered lived-growth Authority aggregate: `PASS`
- Direct corrective gate: `PASS · ACTION×GOAL 9/9 · UNKNOWN=0`
- Semantic selection Receipt V2 / Choice V4 value-closure gate: `PASS`
- Visual-semantic cutover gate: `PASS`
- First-item readiness gate: `PASS`
- Semantic constitution 23-node gate: `PASS · 23/23 · UNKNOWN=0`
- Canonical Authority SHA-256 gate: `PASS`
- Production Fixture/Acceptance/Draft/AI-authoring executable assets: `0`

## Viewport and native motion evidence boundary

The available in-app capture backend did not honor requested native dimensions:

- requested `390×844`, reported DOM `278×603`, physical capture `279×157`;
- requested `320×568`, reported DOM `228×405`, physical capture `188×406`.

The compact DOM reported no horizontal overflow at its actual width. These captures are retained only as diagnostic screenshots and **do not claim native 390×844 or 320×568 acceptance**:

- `XINMAI_SEMANTIC_CONSUMER_COMPLETION_OWNERSHIP_NAMING_DIAGNOSTIC_279x157.jpg`
- `XINMAI_SEMANTIC_CONSUMER_COMPLETION_OWNERSHIP_NAMING_DIAGNOSTIC_188x406.jpg`

The active native preference was Motion (`prefers-reduced-motion: reduce = false`). The environment offered no lawful native system switch; native Reduced Motion remains a `YELLOW — MANUAL DEVICE EVIDENCE REQUIRED`. No emulation is represented as native evidence.

## Counter contract

The direct-child Counter changes only:

`XINMAI_SEMANTIC_CONSTITUTION_EXPERIENCE_POLICY: ENABLED → SAFE_WITHHELD`

It preserves the same modern safe presenter, canonical assets, recovery, Response Map and post-closure Naming. It does not restore old mystical operation copy or a Naming lifecycle gate.
