# XINMAI Six-Dimension Semantic Selection Phase 2 Evidence Manifest

## Build identity

- Expected parent: `6d68e4b12310cc2596a9282de11fc9302d68bd83`
- Candidate policy: `SIX_DIMENSION_SEMANTIC_SELECTION_NEW_MUTATION = ENABLED`
- Production assets: `index-DkWKam9Q.js`, `index-CIsVogs5.css`, `RealityProductionRouteEntry-DpJUsZKd.js`
- Production origin used for the final visible checks: `http://127.0.0.1:5592`
- Development entry assets: 0 (`/@vite/client` and `/src/main.tsx` absent from the production entry)

## Canonical evidence matrix

| Boundary | Result |
| --- | --- |
| New eligible set | V3 only |
| Existing in-progress V2 set | remains V2; existing V2 command path retained |
| Dimension command | dimension ID + bounded semantic response ID + grammar revision + expected revisions |
| Intermediate Presentation | zero writes; only final explicit save issues the command |
| Sixth item | item + set completion + command fence + Receipt V2 in one existing IDB transaction |
| Receipt V2 | six selection references/digests in canonical dimension order + aggregate digest |
| Choice | V4 + Formation Snapshot V3 bound to the exact Receipt V2 aggregate |
| Legacy | V2/Receipt V1/Choice V3 stay read-only; presenter returns generic-only and never invents choices |
| DB topology | physical version 3; 9 stores; 14 indexes; no new DB/store/index |

The final clean visible journey persisted and recovered the bounded sequence `LOCATED / CLEAR / SENTENCE / ADVANCE / THEN / NEED`. At 6/6 the response map mirrored all six choices, showed the selected Reality trigger, and formed the causal sentence from the persisted ACTION and GOAL selections plus the selected micro-action. Receipt remained absent through item 5 and one Receipt V2 was resolved after item 6. The ordinary click control, not the expressive long-press control, committed Choice V4.

## Visible Production checks

- Birth `1990-01-15 10:00` → Genesis → Reality → Dynamics: PASS.
- Each dimension was selected and saved through its own visible, final action: PASS.
- Response map after 6/6 and after recovery: PASS.
- Return showed the selected Reality, six-item map, causal sentence, and carried micro-action: PASS.
- Return naming fallback after an admitted response: absent.
- `NOT_ATTEMPTED`: two truthful actions, zero Fact/Crystal mutation, no public internal jargon: PASS.
- Prior clean positive run on the same Candidate contents: `ATTEMPTED` → one Fact → one Formation Receipt → one Crystal → one Body Imprint → Ownership → fresh legal Reality: PASS.
- Authority/gate harness covers `COMPLETED_AS_INTENDED`, `CHANGED_RESPONSE`, `USER_REJECTED_RECORD`, replay, concurrent item settlement, concurrent sixth item, stale revision, semantic conflict, abort and corrupt evidence.
- Runtime application error/warning log during visible journey: 0.

## Viewport and accessibility evidence

- `01-response-map-requested-390x844-capped-279x603.jpg`: browser CSS viewport requested at 390×844; the capture backend physically capped the exported image at 279×603.
- `02-response-map-requested-320x568-capped-188x406.jpg`: browser CSS viewport requested at 320×568; the capture backend physically capped the exported image at 188×406.
- `03-not-attempted-requested-320x568-capped-229x406.jpg`: the same capped backend shows the no-fact decision hierarchy with two visible actions.
- Semantic DOM exposed progress, current question, selection mirror, save readiness, response-map region and checkpoint groups.
- Every directly operated action was a named button/radio/textbox and keyboard-operable by native semantics.
- DOM reflow checks at requested 390×844 and 320×568 exposed the same facts/actions and no horizontal-overflow failure. The exported raster is not valid native-device-size proof because the available capture backend downscaled/capped it. Native 390×844, native 320×568, and native OS Reduced Motion capture remain yellow manual-device evidence items; they were not emulated or represented as complete.
- Motion facts/actions are CSS-independent and the Reduced Motion contract remains enforced by the registered visual-semantic Gate. Native OS Reduced Motion switching was not changed or emulated in this worktree.

## Engineering evidence

- TypeScript: PASS.
- Production build: PASS.
- Direct Phase 2 Gate: PASS.
- Full registered XINMAI lived-growth aggregate: PASS.
- Release aggregate: reaches the exact-parent pre-existing `mother-code-profile-persistence-semantics` failure (`Launch delegates profile persistence missing=writeMotherCodeProfile(motherHandoff.motherCodeProfile)`); no new Phase 2 failure precedes it.
- `git diff --check`: PASS.
- Production executable Fixture/Acceptance/Draft/AI/reviewer metadata: rejected by registered gates; none added by this Candidate.

## Authority zero drift

Birth, Identity, Relationship, Naming, Pressure Catalog 450, Growth Fact, Eligibility, Formation, Crystal and Body Imprint writers are unchanged. Choice behavior/action routing is unchanged; only the audited V4 evidence binding is activated. No raw whisper, thought text, label, naming text, page-local count or Storage compensation enters canonical evidence.
