# XINMAI 1.0 — Life Presence Scene Projection Calibration / Four-Symbol Visual Consumer Release Gate Ownership Re-audit P0

## Verdict

**A. CORRECTIVE APPLICATION READY — C-CLASS BROAD LEXICAL ASSERTION MUST RETIRE**

Audit only. P101 Gate, Life Presence projection, shared Renderer, visual calibration, product Consumer, Runtime, Authority, DB, Store, schema, Catalog, page, route, and CSS are unchanged in this blade.

## Earliest failure

`check-life-presence-scene-projection-calibration` reports:

`P101 renderer remains identity blind forbidden=fourSymbol`

The Gate searches the complete shared Renderer source for the substring `fourSymbol`. The Renderer now legally consumes:

`sceneProjection.fourSymbolDirectionFieldVisualCalibration`

That typed projection was introduced by the reviewed direction-field visual calibration chain. It is downstream visual input; it is not a call to the Four-Symbol identity engine and does not infer Star Beast identity.

## Ownership decision

The failing assertion is **C — Legacy Presentation / Prototype Detail**.

Identity blindness remains a Canonical Fact:

- no Star Beast resolver;
- no Mother Code engine;
- no Life Archetype inference;
- no scene-model identity input;
- no `mansionName` or `animalIdentity` label;
- no animal-symbol hardcoding;
- no Storage ownership.

A broad substring prohibition cannot distinguish those upstream identity dependencies from a reviewed downstream visual-calibration type. The corrective must retire the broad lexical check while retaining the specific identity-owner exclusions.

## Actual Consumer boundary

`projectPersonalStarBeastRenderPlanToLifePresence` remains consumed by:

- `src/services/personalStarBeastLifePresenceProjection.ts`
- `src/renderers/genesisWebGLRendererCore.ts`
- `src/components/RealityLifeUniverseCanvas.tsx`

The projection service itself remains RenderPlan-only. The shared Renderer additionally accepts typed visual calibration inputs. These are separate contracts and must not be collapsed into one substring rule.

## Full Gate classification

All 91 expanded logical assertions are classified in:

`docs/release-gates/LIFE_PRESENCE_SCENE_PROJECTION_CALIBRATION_ASSERTION_CLASSIFICATION_REGISTER_P0.md`

| Class | Count | Disposition |
|---|---:|---|
| A — Canonical Fact | 42 | KEEP |
| B — Consumer Contract | 31 | KEEP |
| C — Legacy Presentation / Implementation Detail | 18 | RETIRE from Release ownership enforcement |
| D — Unknown / Ambiguous | 0 | None |
| Total | 91 | Fully classified |

## Frozen corrective scope

The next corrective may modify only:

- `scripts/check-life-presence-scene-projection-calibration.mjs`
- corrective evidence documentation

It must:

1. retain all 42 A-class Canonical Facts;
2. retain all 31 B-class Consumer Contracts;
3. retire all 18 C-class exact protocol/Three.js/lexical implementation assertions;
4. preserve projection determinism, A/B differentiation, scene handoff, RenderPlan-only service ownership, non-mutation, identity blindness, and no-animal-geometry behavior;
5. run the dedicated Gate and full Release lifecycle;
6. stop at the next independent owner failure.

It must not:

- edit the Life Presence projection service, Renderer, calibration types, or Consumers;
- remove specific identity-engine/identity-label prohibitions;
- rename or suppress `fourSymbolDirectionFieldVisualCalibration`;
- modify Runtime, Authority, DB, Store, schema, Catalog, pages, routes, or CSS;
- push or promote.

## Next exact blade

`XINMAI-1.0-LIFE-PRESENCE-SCENE-PROJECTION-CALIBRATION-FOUR-SYMBOL-VISUAL-CONSUMER-RELEASE-GATE-OWNER-MODERNIZATION-CORRECTIVE-P0`

Traffic: **YELLOW / GATE EVIDENCE ONLY**

Expected parent: this audit commit

Push: **HOLD**
