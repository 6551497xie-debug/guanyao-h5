# MID_LIFE Runtime Semantic Binding Authoring Report P0

Blade: `XINMAI-1.0-MID-LIFE-PRESSURE-CANDIDATE-90-RUNTIME-SEMANTIC-AND-DUAL-ID-BINDING-PACK-AUTHORING-P0`

Status: `BINDING_DRAFT / AI_ASSISTED / NOT REVIEWED / NOT PRODUCTION`

## Authoring boundary

- Binding author: `XINMAI_CODEX_BINDING_AUTHORING_WINDOW`
- Authoring date: `2026-08-10`
- Independent binding reviewer: `null`
- Review decision / approval / lock: `null / null / null`
- Source: locked MID_LIFE Content Pack A 90, manifest digest `sha256:53a90d7aa60ea7b4850d58fd23e7ad40e8f7751d7ab11b836229e2cf11430b56`
- Runtime, Catalog, Adapter, Gate, Page, Schema and Store changes: `0`

This is semantic binding authorship, not independent review. Existing content reviewers are not reused as binding reviewers, and no record is marked accepted.

## Slot and dual-ID method

Each field follows the explicit context order in the locked Coverage Matrix. Within every context, slots follow:

```text
RULE_OR_INFORMATION_GAP
→ POWER_OR_RESOURCE_ASYMMETRY
→ BOUNDARY_OR_COMMITMENT_BREAK
```

Every slot is recorded explicitly. No slot uses source array order, surface text, hash or pressureNature sorting. Runtime IDs are generated only from `MID_LIFE + field + explicit two-digit slot`; authoring stable IDs remain provenance-only.

## Semantic method

Each `pressureNature` was authored item by item according to the pressure that downstream Choice and Formation should carry. Mechanics and fields were treated as evidence, never as deterministic mappings. `SURVIVAL` was reserved for a direct minimum-life baseline and therefore appears only once; it is explicitly open for independent review because it triggers Choice `SAFE_WITHHELD`.

## Mechanical author self-check (not acceptance)

- source manifest records found: 90/90
- stable ID and content hash copied from lock manifest: 90/90
- source surface/shell copied unchanged as review snapshots: 90/90
- fields: six × 15
- explicit slots: 01..15 exactly once per field
- Runtime IDs: 90 unique
- authoring stable ID ↔ Runtime ID: 90/90 bijection
- pressureNature values: 90/90 in the frozen eight-value domain
- non-empty binding reasons: 90/90
- exact duplicate binding reasons: 0
- workflow: 90/90 `BINDING_DRAFT`
- independent reviewer/review date/decision/approval/lock: 90/90 null
- production eligible: 90/90 false

## ESTABLISHING semantic comparison

This comparison is author-side calibration only:

- public judgment scenes remain `EVALUATION`, consistent with legacy ESTABLISHING public/manager evaluation examples;
- compensation and financial access generally remain `RESOURCE`, consistent with legacy INTEREST, while two explicit disposition-right cases are drafted `CONTROL`;
- bilateral connection is drafted `ATTACHMENT`, consistent with legacy RELATION, but unilateral representation and shared-decision cases are separated into `CONTROL`;
- group access and visibility are drafted `BELONGING`, consistent with legacy SOCIAL, while explicit task reciprocity can be `OBLIGATION`;
- MID_LIFE role-continuity items introduce draft `IDENTITY`; legacy ESTABLISHING has `IDENTITY = 0`, so all five require independent scrutiny rather than imitation of the legacy distribution;
- the single draft `SURVIVAL` is narrower than ordinary money pressure and is listed as an open high-impact risk.

## Open outcome

All semantic uncertainty remains in the separate risk register. No risk is closed by the author.
