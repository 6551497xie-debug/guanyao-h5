# XINMAI Ownership Interactive / Decorative Accessibility Layering Green Correction Closure P0

## Decision

`DELIVERY CLOSED / PASS`

The Ownership accessibility-layering defect is closed as a two-file structural correction. This does not close the V5 Desktop Release Evidence Gate, Phase 3 or the Physical Android Release Gate.

## Delivered chain

- MAP: `7ede0c8ae6926c5e3ee40ced6a109d0c7dd36da5`
- Delivered Runtime: `176b67e9a01d308cc13b41a5c9be06c694a9c25b`
- Local-only direct-child Forward Counter: `29d0e532d0caf0914a76a50c0a9a13358790092f`

The Runtime changes only:

1. `src/components/XinmaiCrystalFormationOwnershipMoment.tsx`
2. `scripts/check-xinmai-crystal-ownership-presentation.mjs`

Ownership state, Growth Authority, Formation success, Storage, schema, Renderer, Scene Host, CSS and product copy remain unchanged.

## Closed defect

The native Crystal button was nested under an `aria-hidden=true` scene wrapper. The delivered correction keeps that wrapper semantically neutral, marks its visual fields and SVG individually decorative, and preserves one real native button as the sole accessible interaction owner.

The registered Ownership gate now rejects:

- an `aria-hidden` scene ancestor around the button;
- removal of the stable button name or `aria-pressed` state;
- more than one Crystal button;
- exposed decorative formation fields;
- a separately labeled or role-bearing Crystal SVG.

## Independent remote-snapshot verification

An independent clean snapshot at the delivered Runtime SHA established:

- TypeScript + Production Build: PASS, 383 modules.
- Hashed Production assets: `index-DuDkAWPy.js`, `index-CLpG8E7C.css`.
- XINMAI gates: 117/117 PASS.
- Vite dev, Acceptance, Fixture, Harness and fault-injection production assets: 0.
- Exactly one button named `轻触这颗 Crystal，确认它来自这次现实回应`.
- The button is native, enabled, visible, stateful and has no hidden ancestor.
- Its SVG is decorative (`aria-hidden=true`, `focusable=false`) with no image role or duplicate name.
- Formation `crystal-formation:1mqc2md` and Crystal `crystal:h6jew7` recover from `CANONICAL_RECOVERY` with `IDB_TRANSACTION_COMPLETE` authority.
- The accessibility tree exposes the Crystal button once and the continue button once.
- Candidate, Counter and closure snapshots are CLEAN after artifact cleanup.
- Main worktree 34 user changes and `public/brand/` remain untouched.

Evidence is stored in:

`/Users/xieyanjun/.codex/visualizations/2026/07/22/019f87bd-b535-7723-9184-06a6fd71127e/ownership-accessibility-layering-green-p0/`

## Counter

The Counter changes only `xinmaiReturningSameLifeContinuityPresentationPolicy.ts` from `ENABLED` to `SAFE_WITHHELD`. Counter Build and 117/117 gates pass. Its hashed Production evidence keeps the existing Ownership, Formation, Crystal, success authority and single named native Crystal button while withholding only the V4 semantic projection. It remains local only.

## Remaining V5 evidence boundary

- Browser semantic-tree proof: PASS.
- Native VoiceOver: not run; the service was not active.
- Native macOS Reduced Motion: current setting was Motion (`reduceMotion=0`); no setting was changed in this correction.
- Native 200% and configurable 320–430 px Desktop capture: not available through the current in-app browser evidence channel.
- Physical Android TalkBack / heat / final performance: remains the Phase 3 Android Release Gate.

These are retained as explicit V5 evidence gaps. They do not reopen the closed attribute-layering defect, and they are not represented as passing evidence.

## Stage state

- Ownership accessibility layering: CLOSED / PASS
- V5 Desktop Release Evidence Gate: OPEN
- Physical Android Release Gate: DEFERRED FINAL EVIDENCE
- C1 / C2: CLOSED / PASS
- Phase 3: ACTIVE / NOT PASSED
- C3 Audio/Haptic: PAUSED / DEFER
- AI, Research, Monetization: DEFER
- Phase 4: LOCKED
