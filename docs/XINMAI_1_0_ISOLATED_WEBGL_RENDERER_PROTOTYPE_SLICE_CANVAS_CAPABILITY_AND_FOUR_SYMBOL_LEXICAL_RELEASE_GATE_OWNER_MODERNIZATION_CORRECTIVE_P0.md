# XINMAI 1.0 — Isolated WebGL Renderer Prototype Slice Canvas Capability and Four-Symbol Lexical Release Gate Owner Modernization Corrective P0

## Exit

**A. CORRECTIVE COMPLETE — P99 GATE PASSES; RELEASE ADVANCED TO P101 INDEPENDENT OWNER FAILURE**

This corrective changes only `scripts/check-isolated-webgl-renderer-prototype-slice.mjs` and evidence documentation. Renderer, Canvas input type, prototype facade, product Consumer, page, route, CSS, Runtime, Authority, DB, Store, schema, and Catalog are unchanged.

## Applied ownership corrections

The Gate now retains 103 Release-owned assertions:

- A — Canonical Fact: 41
- B — Consumer Contract: 62
- D — Unknown: 0

It retires 35 C-class legacy P98/P99 implementation/protocol assertions.

The no-WebGL Canvas double now implements:

```js
{
  hasAttribute: () => false,
  getContext: () => null,
}
```

This represents a valid non-Reality, non-Life-Origin Canvas capability boundary and allows the shared Renderer to return the typed `WEBGL2_UNAVAILABLE` fallback. Formal Canvas detection is not weakened.

The broad `fourSymbol` substring ban is retired. Specific prohibitions on direct identity engines, scene-model input, Mother Code, identity labels, animal labels, and Storage remain intact.

## Dedicated verification

`npm run check-isolated-webgl-renderer-prototype-slice`

Result: **PASS**

Verified behavior includes:

- identity-blind deterministic projections;
- distinct projection response to distinct plans;
- missing Canvas → `CANVAS_REQUIRED`;
- Reduced Motion → `REDUCED_MOTION_REQUESTED`;
- Canvas without WebGL2 → `WEBGL2_UNAVAILABLE`;
- invalid viewport/pixel ratio → `VIEWPORT_INVALID`;
- unbound RenderPlan → `RENDER_PLAN_NOT_AUTHORIZED`.

## Release lifecycle result

`npm run check:release` passed the corrected P99 Gate and advanced into `postcheck:release`.

It stopped at the next independent Gate:

`check-life-presence-scene-projection-calibration`

Reproduction result:

`P101 renderer remains identity blind forbidden=fourSymbol`

P101 independently owns another broad lexical assertion that now collides with the same reviewed `fourSymbolDirectionFieldVisualCalibration` Consumer. It is outside this P99 corrective and was not modified.

## Scope proof

Implementation change:

- `scripts/check-isolated-webgl-renderer-prototype-slice.mjs`

Evidence change:

- this document

Product source and all formal Runtime/Authority/Renderer/Consumer files: **0 diff**

Push: **HOLD**

## Next exact audit

`XINMAI-1.0-LIFE-PRESENCE-SCENE-PROJECTION-CALIBRATION-FOUR-SYMBOL-VISUAL-CONSUMER-RELEASE-GATE-OWNERSHIP-RE-AUDIT-P0`

Traffic: **YELLOW / AUDIT FIRST**

The audit must distinguish identity inference from typed downstream visual calibration and classify every P101 Release assertion before any Gate edit.
